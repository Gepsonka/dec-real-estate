export interface RealEstateToken {
  tokenId: string; // tokenId
  supply: BigInt;
  creator: `0x${string}`; // wallet address
  tokenName: string;
  decsription: string;
  createdAt: number;
  updatedAt: number;
}
